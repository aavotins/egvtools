% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/egvtools-package.R
\docType{package}
\name{egvtools}
\alias{egvtools}
\alias{egvtools-package}
\title{egvtools: High-resolution Ecogeographical Variable Workflows}
\description{
\code{egvtools} provides a coherent set of wrappers and utilities that make
large-scale \strong{EGV} creation reproducible and pleasant on real datasets.
The package leans on robust building blocks—\code{terra}, \code{sf}, \code{sfarrow},
\code{exactextractr}, and \code{whitebox}—and standardizes I/O, naming conventions,
and multi-scale zonal statistics so your pipelines are repeatable across machines
and projects.
}
\section{Core workflow (generalizable)}{

Functions that form the backbone of your analyses (thin wrappers over
existing libs, but opinionated for stability, speed and consistency):
\itemize{
\item \code{polygon2input()} — rasterize polygons to template, handle background/mask.
\item \code{downscale2egv()} — downscale coarse rasters to template grid and optionally smooth with IDW.
\item \code{distance2egv()} — distances to features with optional gap filling at the edges.
\item \code{input2egv()} — normalize/align inputs to EGV outputs with guards.
\item \code{landscape_function()} — \pkg{landscapemetrics} landscape-level per-zone metrics, tiled.
\item \code{radius_function()} — multi-scale zonal statistics (dense/sparse).
}
}

\section{Reproducibility helpers}{

Utilities that set up inputs and structure so our (project HiQBioDiv) results
can be reproduced:
\itemize{
\item \code{download_raster_templates()} — fetch template rasters (Zenodo) to
canonical paths.
\item \code{download_vector_templates()} — fetch template vector grids/points.
\item \code{tile_vector_grid()} — tile template grids for chunked processing.
\item \code{tiled_buffers()} — precompute buffered tiles for multiple radii.
\item \code{create_backgrounds()} — build consistent background rasters/values.
}
}

\section{Design principles}{

\itemize{
\item \strong{Tiled, RAM-aware I/O}; workers open data by path (avoid big globals) and
caches them if necessary (avoid big I/O).
\item \strong{Deterministic filenames} and strict layer/radius naming.
\item \strong{Guards} for empty/invalid geometries, coordinate reference systems, naming and all-NA joins.
\item \strong{Cross-platform parallel} via \code{{future}/{furrr}} when enabled.
}
}

\section{Options}{

Package defaults are set on load (see \code{zzz-options.R}). Users may override:
\itemize{
\item \code{options(egvtools.future_plan = "sequential")}
\item \code{options(egvtools.progress    = TRUE)}
}
}

\section{Getting started}{

\enumerate{
\item Run \verb{download_*_templates()} to fetch canonical inputs.
\item Use \code{tile_vector_grid()} / \code{tiled_buffers()} for scalable chunks.
\item Produce EGVs at site scale with \code{polygon2input()}, \code{downscale2egv()}, \code{distance2egv()},
\code{input2egv()}, and summarise from larger scales with \code{landscape_function()} / \code{radius_function()}.
}
}

\seealso{
Useful links:
\itemize{
\item GitHub repo: \url{https://github.com/aavotins/egvtools}
\item Package site: \url{https://aavotins.github.io/egvtools/}
\item Issues / bugs: \url{https://github.com/aavotins/egvtools/issues}
}
}
\author{
\strong{Maintainer}: Andris Avotiņš \email{andris.avotins@lu.lv}

}
\keyword{internal}
