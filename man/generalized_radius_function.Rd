% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generalized_radius_function.R
\name{generalized_radius_function}
\alias{generalized_radius_function}
\title{Extract and Rasterize Buffered-Radius Summary Statistics}
\usage{
generalized_radius_function(
  kvadrati_path,
  radii_path,
  reference_grid_path = NULL,
  template_path,
  input_layers,
  layer_prefixes,
  output_dir = "./Extracted_Layers",
  unlink_tiles = TRUE,
  n_workers = 1,
  radii = c("r500", "r1250", "r3000", "r10000"),
  fill_missing = TRUE,
  radius_mode = "sparse",
  IDW_weight = 2,
  extract_fun = "mean",
  future_max_size = 8 * 1024^3,
  gdal_opts = c("COMPRESS=LZW", "TILED=YES", "BIGTIFF=IF_SAFER", "NUM_THREADS=ALL_CPUS",
    "BLOCKXSIZE=256", "BLOCKYSIZE=256"),
  write_datatype = NULL,
  NAflag = NULL,
  terra_memfrac = 0.7,
  terra_tempdir = tempdir(),
  terra_todisk = TRUE,
  quiet = FALSE,
  buffer_mode = NULL,
  buffer_spec = NULL,
  tikls100_path = NULL,
  crop_buffer = 1000
)
}
\arguments{
\item{kvadrati_path}{Character. Directory containing tiled GeoParquet files
for the base rasterization grid. The tile identifier is taken from the
final underscore-separated component of each file name, before the file
extension. For example, \code{grid_2434.parquet} has tile ID \code{2434}.}

\item{radii_path}{Character. Directory containing tiled buffered-polygon
GeoParquet files. Fixed-radius files are expected to follow the naming
convention \verb{<buffer_grid>_r<radius>_<tileid>.parquet}, matching the output
convention of \code{\link[=tiled_buffers]{tiled_buffers()}}.}

\item{reference_grid_path}{Character or \code{NULL}. Optional path to a
GeoParquet rasterization grid used by the built-in \code{"sparse"} mode for
coarser buffer grids. The grid must contain the relevant \code{key_field}
values. It is not required for \code{"dense"} mode or when a custom
\code{buffer_spec} supplies all required grid paths.}

\item{template_path}{Character. Path to the template raster defining final
extent, resolution, alignment, mask, and CRS.}

\item{input_layers}{Named or unnamed character vector of raster paths from
which summary statistics are extracted. Names are replaced by
\code{layer_prefixes}.}

\item{layer_prefixes}{Character vector with one output prefix for each
element of \code{input_layers}. Values must be unique.}

\item{output_dir}{Character. Output directory root. Default
\code{"./Extracted_Layers"}.}

\item{unlink_tiles}{Logical. If \code{TRUE}, delete intermediate per-tile raster
directories after successful mosaicking. Default \code{TRUE}.}

\item{n_workers}{Integer. Number of parallel workers. On SLURM systems this
is capped at the detected CPU allocation. Default \code{1}.}

\item{radii}{Character or numeric vector selecting radii to process. Numeric
values are interpreted as distances and converted to IDs such as \code{500} to
\code{"r500"}. Character values may be supplied either as \code{"500"} or
\code{"r500"}. In \code{"sparse"} and \code{"dense"} modes the default processes
\code{r500}, \code{r1250}, \code{r3000}, and \code{r10000}. In \code{"specified"} mode, if
\code{radii} is omitted, all rows of \code{buffer_spec} are processed.}

\item{fill_missing}{Logical. If \code{TRUE}, fill NA gaps within the template
footprint using \code{whitebox::wbt_fill_missing_data()}. Default \code{TRUE}.}

\item{radius_mode}{Deprecated compatibility argument corresponding to the
former \code{radius_mode}. Prefer \code{buffer_mode}. If \code{buffer_mode} is \code{NULL},
this value is used. Default \code{"sparse"}.}

\item{IDW_weight}{Numeric. Inverse-distance weighting power passed to
\code{whitebox::wbt_fill_missing_data()}. Default \code{2}.}

\item{extract_fun}{A function or a single character string such as
\code{"mean"}, passed to \code{exactextractr::exact_extract()}. It must resolve to
exactly one numeric value per polygon and raster layer.}

\item{future_max_size}{Numeric. Maximum size, in bytes, of globals exported
to each future worker. Default \code{8 * 1024^3}.}

\item{gdal_opts}{Character vector of GDAL creation options used for raster
writes. By default LZW compression, tiling, BigTIFF-if-needed, and 256-cell
blocks are used. Threading is reduced to one GDAL thread per worker when
parallel processing is active.}

\item{write_datatype}{Character or \code{NULL}. Optional terra datatype such as
\code{"FLT4S"} or \code{"INT2S"}. Default \code{NULL} uses terra's default.}

\item{NAflag}{Numeric or \code{NULL}. Optional NA flag used when writing rasters.
Default \code{NULL}.}

\item{terra_memfrac}{Numeric. Fraction passed to
\code{terra::terraOptions(memfrac = ...)}. Default \code{0.7}.}

\item{terra_tempdir}{Character. Temporary directory used by terra and for
intermediate mosaics. Default \code{tempdir()}.}

\item{terra_todisk}{Logical. If \code{TRUE}, prefer on-disk terra operations.
Default \code{TRUE}.}

\item{quiet}{Logical. If \code{TRUE}, suppress progress messages. Default
\code{FALSE}.}

\item{buffer_mode}{Character or \code{NULL}. Preferred replacement for
\code{radius_mode}. One of \code{"sparse"}, \code{"dense"}, or \code{"specified"}.
\itemize{
\item \code{"sparse"} reproduces the package's standard sparse setup:
\code{pts100} at 500 and 1250 m, \code{pts300} at 3000 m, and \code{pts1000} at
10000 m. The 500 and 1250 m results join directly to each tile by \code{id};
the 3000 and 10000 m results use \code{reference_grid_path} with
\code{rinda300} and \code{ID1km}, respectively.
\item \code{"dense"} uses \code{pts100} for all four standard radii and joins every
result directly to the current tile by \code{id}.
\item \code{"specified"} uses \code{buffer_spec} and contains no built-in assumptions
about buffer-grid names, radii, or key fields.
}

If \code{NULL}, the value of the compatibility argument \code{radius_mode} is used.}

\item{buffer_spec}{A data.frame used when \code{buffer_mode = "specified"}.
It must contain exactly one processing rule per radius and the columns:
\itemize{
\item \code{buffer_grid}: character prefix identifying buffered files. For example,
\code{"pts250"} matches files such as \code{pts250_r2000_2434.parquet}.
\item \code{radius_m}: positive whole-number buffer radius used to construct the
radius ID \verb{r<radius_m>} and locate buffered files.
\item \code{key_field}: character name of the field that links each buffered
polygon to the grid on which its extracted value is rasterized.
\item \code{grid_path}: character path to a GeoParquet rasterization grid, or
\code{NA}/\code{"tile"} to use the current tile from \code{kvadrati_path}.
}

Each radius must occur only once because each layer-radius combination
produces one output raster.}

\item{tikls100_path}{Deprecated compatibility alias for
\code{reference_grid_path}. If supplied, \code{reference_grid_path} must be \code{NULL}.}

\item{crop_buffer}{Numeric. Additional map-unit buffer around the bounding
box of the largest available buffered polygons when cropping input rasters
and the template. Default \code{1000}.}
}
\value{
Invisibly returns a data.frame with one row per successfully written
layer-radius combination and columns:
\itemize{
\item \code{layer}: output layer prefix;
\item \code{radius}: radius ID such as \code{"r3000"};
\item \code{radius_m}: numeric radius;
\item \code{buffer_grid}: buffered-file grid prefix;
\item \code{output_path}: final raster path;
\item \code{n_tiles_merged}: number of tile rasters included in the mosaic;
\item \code{gaps_before}: number of NA cells inside the template before filling;
\item \code{gaps_after}: number remaining after optional filling;
\item \code{filter_size_used}: Whitebox filter width, or \code{NA} if not used; and
\item \code{gap_filled}: logical indicating whether gap filling was successfully
applied.
}
}
\description{
Extracts one summary statistic per buffered polygon from one or more raster
layers, transfers those values to a rasterization grid using a user-defined
key field, rasterizes the results to a common template, and merges the
per-tile outputs into final rasters.

The relationship between buffered point grids, radii, join keys, and
rasterization grids is controlled by \code{buffer_mode} and, for fully custom
workflows, \code{buffer_spec}. This avoids hard-coding particular grid names or
key fields in the extraction logic.
}
\details{
\subsection{Buffer specification}{

Internally all three modes are converted to the same four-column
specification: \code{buffer_grid}, \code{radius_m}, \code{key_field}, and \code{grid_path}.
Consequently, the extraction and rasterization code does not contain
radius-specific rules.

If \code{grid_path} is \code{NA}, empty, or \code{"tile"}, extracted values are joined to
the current tiled grid from \code{kvadrati_path}. Otherwise the referenced
GeoParquet file is loaded once per worker, filtered to the key values present
in the current buffer polygons, and used as the rasterization grid.

The \code{key_field} must exist in both the buffered polygons and the selected
rasterization grid. It must uniquely identify buffered polygons within a
buffer file. It may be repeated in the rasterization grid; this is what
permits a statistic calculated for a coarser buffer center to be assigned to
multiple finer output cells.
}

\subsection{Workflow}{
\enumerate{
\item Resolve \code{buffer_mode} to a validated buffer specification.
\item Discover base-grid tiles and buffered files using their file-name
conventions.
\item For each tile, read the available buffered polygons and identify the
largest available radius.
\item Crop the template and raster stack to the largest buffered extent plus
\code{crop_buffer}.
\item Run \code{exactextractr::exact_extract()} once per radius over the cropped
raster stack.
\item Join each extracted statistic to the rule's rasterization grid using
\code{key_field}, rasterize with \code{fasterize::fasterize()}, and write per-tile
GeoTIFFs.
\item VRT-mosaic the per-tile rasters, project and mask them to \code{template_path},
optionally fill internal NA gaps, and write the final GeoTIFF.
}
}

\subsection{Parallel and memory behavior}{

Multisession futures are used rather than forking. Large reusable objects,
including the template, input raster stack, and external rasterization grids,
are cached once per worker through the internal egvtools worker cache. Cache
keys include the normalized source paths (and raster layer names where
relevant), preventing consecutive calls with different inputs from reusing
stale file-backed raster objects. BLAS, OpenMP, and GDAL thread counts are
constrained inside workers to reduce oversubscription on HPC systems.
}

\subsection{Gap filling}{

After mosaicking and alignment, gaps are defined as NA cells inside the
non-NA template footprint. When \code{fill_missing = TRUE}, the maximum distance
into the gap mask is used to derive an odd Whitebox filter width with a
minimum of three cells. Gap counts before and after filling are returned.
}
}
\examples{
\dontrun{
# Standard sparse workflow.
generalized_radius_function(
  kvadrati_path = "./Templates/TemplateGrids/lapas/",
  radii_path = "./Templates/TemplateGridPoints/lapas/",
  reference_grid_path =
    "./Templates/TemplateGrids/tikls100_sauzeme.parquet",
  template_path = "./Templates/TemplateRasters/LV100m_10km.tif",
  input_layers = c(
    Soils_txtSand = "./Rastri_100m/RAW/Soils_txtSand_cell.tif",
    Soils_txtSilt = "./Rastri_100m/RAW/Soils_txtSilt_cell.tif"
  ),
  layer_prefixes = c("Soils_txtSand", "Soils_txtSilt"),
  buffer_mode = "sparse",
  n_workers = 4
)

# Fully custom relationship between buffered files and rasterization grids.
spec <- data.frame(
  buffer_grid = c("pts100", "pts100", "pts300", "pts1000"),
  radius_m = c(500, 1250, 3000, 10000),
  key_field = c("id", "id", "rinda300", "ID1km"),
  grid_path = c(
    NA,
    NA,
    "./Templates/TemplateGrids/tikls100_sauzeme.parquet",
    "./Templates/TemplateGrids/tikls100_sauzeme.parquet"
  ),
  stringsAsFactors = FALSE
)

generalized_radius_function(
  kvadrati_path = "./Templates/TemplateGrids/lapas/",
  radii_path = "./Templates/TemplateGridPoints/lapas/",
  template_path = "./Templates/TemplateRasters/LV100m_10km.tif",
  input_layers = c(NDVI = "./Rastri_100m/NDVI_cell.tif"),
  layer_prefixes = "NDVI",
  buffer_mode = "specified",
  buffer_spec = spec,
  n_workers = 4
)

# A custom grid can join directly back to the current tile.
custom_spec <- data.frame(
  buffer_grid = "pts200",
  radius_m = 750,
  key_field = "id",
  grid_path = NA_character_,
  stringsAsFactors = FALSE
)
}

}
\seealso{
\code{\link[=tiled_buffers]{tiled_buffers()}}
}
